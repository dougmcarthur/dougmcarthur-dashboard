name = "dougmcarthur-dashboard"
main = "src/index.ts"
compatibility_date = "2026-06-21"
workers_dev = false
preview_urls = false

[[d1_databases]]
binding = "DB"
database_name = "dougmcarthur-music-hq"
database_id = "515d234f-92c3-4519-abc3-3d453a1b5058"
migrations_dir = "migrations"

[assets]
directory = "./dist"
binding = "ASSETS"

# Daily at 13:00 UTC (~08:00 Winnipeg CDT): opens arrived submission windows,
# prepares upcoming applications, and sends due reminder emails.
[triggers]
crons = ["0 13 * * *"]

# NOTE: compatibility_date is a best-effort guess (matches Worker's created_on).
# Verify against the real deployed Worker settings before relying on it.
